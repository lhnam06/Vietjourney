spring:
  main:
    lazy-initialization: true
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0}
    username: ${DB_USERNAME:postgres.jegerdfhcbrcziuuhsbe}
    password: ${DB_PASSWORD:BripbPKL9y4AWz2A}
    hikari:
      minimum-idle: 1
      maximum-pool-size: 10
      connection-timeout: 15000
  flyway:
    baseline-on-migrate: true
    locations: classpath:db/migration
    validate-on-migrate: ${SPRING_FLYWAY_VALIDATE_ON_MIGRATE:false}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
server:
  port: ${PORT:8082}
  address: 0.0.0.0
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000}
place:
  datasource:
    enabled: ${PLACE_DB_ENABLED:true}
    url: ${PLACE_DB_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0}
    username: ${PLACE_DB_USERNAME:postgres.jegerdfhcbrcziuuhsbe}
    password: ${PLACE_DB_PASSWORD:BripbPKL9y4AWz2A}
    hikari:
      minimum-idle: 1
      maximum-pool-size: 10
      connection-timeout: 15000
  lookup:
    food-table: ${PLACE_DB_FOOD_TABLE:places_food}
    drink-table: ${PLACE_DB_DRINK_TABLE:places_drink}
    activity-table: ${PLACE_DB_ACTIVITY_TABLE:places_activity}
    id-column: ${PLACE_DB_ID_COLUMN:id}

jwt:
  signer_key: ${JWT_SIGNER_KEY:JbkeHOoV80MgJ5PuhqeC5sElK3DpLzNr7EExafczUD4oETLYQyEcg28TPug3MyyLy1Y34vDitUqvwjdZLiGZ1Z}
  valid_duration: 36000
  refreshable_duration: 604800
